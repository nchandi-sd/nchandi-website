import { SortPanelMembersByEmailPipe } from './sort-panel-members-by-email.pipe';

describe('SortPanelMembersByEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new SortPanelMembersByEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
